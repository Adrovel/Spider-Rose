# Workflow Blocks

Status: Queued UI note  
Created: 2026-06-05

This note captures UI observations for workflow blocks.

These are not approved implementation tasks. Do not add them to `Atomic-Plan.md` unless Joel explicitly approves each item.

## Notes

1. The blocks should be movable.
2. You should be able to delete the blocks.
3. If there is delay in actions between agents, the UI should show that visually, like a transfer of data.
4. The demo UI should run somewhere else.
5. Blocks should be movable without compromising the links between blocks.
6. The link between blocks should be more refined.
7. The right UI panel should be movable and adjust according to the blocks.
8. The right UI panel should not block the visual element.
9. The right UI panel should hover.
